name: NPM Release

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  npm-release:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22'
        cache: 'npm'
        registry-url: 'https://registry.npmjs.org'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Run tests
      run: npm test
      
    - name: Extract version from release
      id: version
      run: |
        VERSION=${GITHUB_REF#refs/tags/v}
        echo "version=$VERSION" >> $GITHUB_OUTPUT
        echo "NPM Release version: $VERSION"
        
    - name: Update package.json version
      run: |
        npm version ${{ steps.version.outputs.version }} --no-git-tag-version
        
    - name: Publish to NPM
      run: npm publish
      env:
        NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        
    - name: Create GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        files: |
          README.md
          LICENSE
        tag_name: ${{ github.ref_name }}
        name: NPM Release ${{ github.ref_name }}
        body: |
          ## 📦 NPM Package Release ${{ steps.version.outputs.version }}
          
          ### Installation
          ```bash
          npm install -g mcp-echo-input-server@${{ steps.version.outputs.version }}
          ```
          
          ### Usage
          ```bash
          # Using npx (recommended)
          npx -y mcp-echo-input-server@${{ steps.version.outputs.version }}
          
          # Using global installation
          mcp-echo-server
          ```
          
          ### Claude Desktop Configuration
          ```json
          {
            "mcpServers": {
              "echo-server": {
                "command": "npx",
                "args": ["-y", "mcp-echo-input-server@${{ steps.version.outputs.version }}"],
                "env": {
                  "USER_NAME": "YourName"
                }
              }
            }
          }
          ```
          
          ### Features
          - Echo tool with customizable user name
          - Cross-platform support
          - Environment variable configuration
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
